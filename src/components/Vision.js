import React from 'react'

const Vision = (props) => {
  return (
    
        <div className='vision'>
            <h1>{props.title}</h1>
            <p>{props.info}</p>
        </div>
    
  )
}

export default Vision